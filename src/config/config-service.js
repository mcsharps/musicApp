// read only config to be resolved prior to bootstrapping the app
//  you may want to call an API or pull preloaded server side state from the DOM here
export const getConfig = () => Promise.resolve({});
